{
  "config": {
    "step": {
      "user": {
        "title": "Connect Discord guild",
        "description": "Enter the Discord bot token, guild id, and external API key.",
        "data": {
          "bot_token": "Bot token",
          "guild_id": "Guild ID",
          "api_key": "API key"
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect to Discord.",
      "guild_not_found": "The bot cannot access that guild.",
      "invalid_guild_id": "Guild ID must contain digits only.",
      "invalid_auth": "The Discord bot token is invalid.",
      "unknown": "An unexpected error occurred."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Filter channel list",
        "data": {
          "recent_message_limit": "Default recent-message limit",
          "category_filter": "Category filter",
          "channel_kind_filter": "Channel type filter",
          "show_selected_only": "Show enabled channels only"
        }
      },
      "enabled": {
        "title": "Choose enabled channels",
        "data": {
          "enabled_action": "Bulk channel action",
          "enabled_channels": "Enabled channels"
        }
      }
    }
  }
}
